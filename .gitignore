# local editor / OS
.claude/
.DS_Store

# built binaries — regenerated from source by the build scripts
*.scr
*.zip
screensaver/ApplebeeAcres.saver/
screensaver/build/
windows-screensaver/bin/
windows-screensaver/obj/
windows-screensaver/publish/
node_modules/

# secrets — never commit
evolve/auth.env
*.env
*.key
*.pem

# evolve loop runtime artifacts (logs + safety backups)
evolve/*.log
evolve/*.bak

# Info-ZIP in-progress temp files (zi + 6 random chars) — a git add racing a running zip once swept one in
windows-screensaver/zi??????


# maze game — its own project living in Creative; keep it out of the farm repo
maze game/
